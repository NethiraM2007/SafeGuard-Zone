# SafeGuard-Zone
This project is an Arduino-based industrial safety system that monitors a restricted work area. It uses an ultrasonic sensor to detect the distance of a worker and it alert. The safety status is displayed on a 16×2 LCD, while a buzzer provides an immediate warning. Bluetooth can also send an alert to a mobile phone.
